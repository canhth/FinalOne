platform :ios, '9.0'

target 'FinalOne' do
  use_frameworks!

  # Pods for FinalOne
  pod 'CT_RESTAPI', :path => 'LocalPods/CT_RESTAPI/'
  pod 'ViewAnimator', :path => 'LocalPods/ViewAnimator/'
  
  pod 'Alamofire'
  pod 'SwiftLint'
  pod 'Kingfisher'
  pod 'SVProgressHUD'
  
  target 'FinalOneTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FinalOneUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
